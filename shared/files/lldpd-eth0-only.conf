# Restrict lldpd to the physical uplink. wlan0, docker bridges, and
# tailscale0 don't connect to managed switches, so advertising/receiving
# LLDP on them is noise.
configure system interface pattern eth0
