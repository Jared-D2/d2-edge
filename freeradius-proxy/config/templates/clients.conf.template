# --- Plain RADIUS clients (UDP) - existing devices ---------------------------
# One client block per entry in LOCAL_CLIENT_SUBNET (space or comma separated).
# Generated by render-configs.sh.
${LOCAL_CLIENTS_UDP_BLOCKS}

# --- RadSec clients (TCP/TLS) - devices using RADIUS over TLS ---------------
${LOCAL_CLIENTS_RADSEC_BLOCKS}
